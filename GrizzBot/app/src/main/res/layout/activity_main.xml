<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login_background"
    tools:context=".MainActivity">

    <!-- App Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GrizzBot"
        android:textSize="48sp"
        android:textStyle="bold"
        android:layout_marginTop="80dp"
        android:layout_centerHorizontal="true"
        android:textColor="@color/textColor"
        android:id="@+id/nametv"
        android:shadowColor="@android:color/black"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="4" /> <!-- Text shadow for title -->

    <!-- Welcome Message -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to GrizzBot!"
        android:layout_below="@id/nametv"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:textSize="20sp"
        android:textColor="@color/secondaryTextColor"
        android:shadowColor="@android:color/black"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="4" /> <!-- Added shadow for readability -->

    <!-- Start Chat Button -->
    <Button
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/startchatbtn"
        android:layout_marginHorizontal="16dp"
        android:layout_centerInParent="true"
        android:layout_marginBottom="40dp"
        android:text="Start Chat"
        android:textColor="@color/textColor"
        android:backgroundTint="#ECCA94"
        android:textSize="20sp"
        android:paddingVertical="16dp"
        android:fontFamily="sans-serif-medium"
        android:elevation="6dp" /> <!-- Added shadow for depth -->

    <Button
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:id="@+id/historychatbtn"
        android:layout_marginHorizontal="16dp"
        android:layout_centerInParent="true"
        android:layout_marginBottom="40dp"
        android:layout_below="@id/startchatbtn"
        android:text="History of Chat"
        android:textColor="@color/textColor"
        android:backgroundTint="#ECCA94"
        android:textSize="20sp"
        android:paddingVertical="16dp"
        android:fontFamily="sans-serif-medium"
        android:elevation="6dp" />

    <!-- Settings Button -->

</RelativeLayout>
